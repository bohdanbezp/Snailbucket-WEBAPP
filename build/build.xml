<?xml version="1.0" encoding="UTF-8"?>

<!-- Apache Ant build file -->

<!-- The file supports the following targets:
     * "compile" - compiles the project
     * "clean" - removes compiled classes
     * "runserver" - runs the web-server
-->

<project name="RWChess" default="complile" basedir=".">
  <property name="sdk.dir" location="../appengine-java-sdk-1.3.0" />

  <import file="${sdk.dir}/config/user/ant-macros.xml" />

  <path id="project.classpath">
    <pathelement path="../war/WEB-INF/classes" />
    <fileset dir="../war/WEB-INF/lib">
      <include name="**/*.jar" />
    </fileset>
    <fileset dir="${sdk.dir}/lib">
      <include name="shared/**/*.jar" />
    </fileset>
  </path>


  <target name="copyjars"
      description="Copies the App Engine JARs to the WAR.">
      <mkdir dir="../war/WEB-INF/lib" />
      <copy
        todir="../war/WEB-INF/lib"
        flatten="true">
      <fileset dir="${sdk.dir}/lib/user">
        <include name="**/*.jar" />
      </fileset>
    </copy>
  </target>

  <target name="compile" depends="copyjars"
      description="Compiles Java source and copies other source files to the WAR.">
    <mkdir dir="../war/WEB-INF/classes" />
    <copy todir="../war/WEB-INF/classes">
      <fileset dir="../src">
        <exclude name="**/*.java" />
      </fileset>
    </copy>
    <javac
        srcdir="../src"
        destdir="../war/WEB-INF/classes"
        classpathref="project.classpath"
        debug="on" />
  </target>

  <target name="datanucleusenhance" depends="compile"
      description="Performs JDO enhancement on compiled data classes.">
    <enhance_war war="../war" />
  </target>

  <target name="runserver" depends="datanucleusenhance"
      description="Starts the development server.">
    <dev_appserver war="../war" port="8080" />
  </target>

  <target name="clean">
    <delete dir="../war/WEB-INF/classes"/>
    <delete dir="../war/WEB-INF/lib"/>
  </target>

</project>


